{
  "name": "zeami-term",
  "version": "0.1.0",
  "description": "Smart terminal emulator for Claude Code with Zeami integration",
  "main": "src/main/index.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "dev:minimal": "electron minimal-main.js",
    "build": "electron-builder",
    "build:mac": "electron-builder --mac",
    "build:win": "electron-builder --win",
    "build:linux": "electron-builder --linux",
    "dist": "npm run build",
    "rebuild": "electron-rebuild -f -w node-pty",
    "test": "jest",
    "test:direct": "node test/terminal.test.js",
    "test:pty": "node test-pty.js",
    "lint": "eslint src --ext .js,.jsx",
    "type-check": "echo 'TypeScript not yet configured'"
  },
  "keywords": [
    "terminal",
    "electron",
    "zeami",
    "claude-code"
  ],
  "author": "Zeami Team",
  "license": "MIT",
  "devDependencies": {
    "electron": "^28.1.0",
    "electron-builder": "^26.0.12",
    "electron-rebuild": "^3.2.9",
    "eslint": "^8.56.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "@xterm/addon-canvas": "^0.7.0",
    "@xterm/addon-serialize": "^0.13.0",
    "@xterm/addon-webgl": "^0.18.0",
    "node-pty": "^0.10.1",
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0",
    "xterm-addon-search": "^0.13.0",
    "xterm-addon-web-links": "^0.9.0"
  }
}
